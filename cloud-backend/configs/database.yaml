# 数据库配置文件 - 生产环境连接信息
database:
  # MySQL 8.0.31 配置
  mysql:
    host: "dbconn.sealosbja.site"
    port: 36973
    username: "root"
    password: "hxl202653"
    database: "ycgcloud"
    charset: "utf8mb4"
    parse_time: true
    loc: "Local"
    timeout: "10s"
    read_timeout: "30s"
    write_timeout: "30s"
    
    # 连接池配置
    max_open_conns: 100
    max_idle_conns: 10
    conn_max_lifetime: "1h"
    conn_max_idle_time: "10m"

  # MongoDB 6.0 配置
  mongodb:
    uri: "mongodb://root:hxl202653@dbconn.sealosbja.site:42033/?directConnection=true"
    database: "ycgcloud"
    username: "root"
    password: "hxl202653"
    connect_timeout: "10s"
    socket_timeout: "30s"
    server_sel_timeout: "30s"
    
    # 连接池配置
    max_pool_size: 100
    min_pool_size: 5
    max_idle_time_ms: 300000  # 5分钟

  # Redis 7.0+ 配置
  redis:
    host: "dbconn.sealosbja.site"
    port: 49169
    password: "jdsj6j67"
    db: 0
    username: "default"
    
    # 连接池配置
    pool_size: 20
    min_idle_conns: 5
    max_conn_age: "1h"
    pool_timeout: "4s"
    idle_timeout: "5m"
    idle_check_freq: "1m"
    
    # 超时配置
    dial_timeout: "5s"
    read_timeout: "3s"
    write_timeout: "3s"

  # 全局配置
  max_conns: 100
  max_idle: 10